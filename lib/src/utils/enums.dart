enum PodVideoState {
  loading,
  playing,
  paused,
  error,
}

enum VideoFit {
  contain,
  cover,
}

enum PodVideoPlayerType {
  network,
  networkQualityUrls,
  file,
  asset,
  vimeo,
  youtube,
}
